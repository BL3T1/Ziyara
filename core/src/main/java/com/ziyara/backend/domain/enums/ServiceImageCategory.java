package com.ziyara.backend.domain.enums;

/**
 * Classification for service gallery images (property, room, trip, etc.).
 */
public enum ServiceImageCategory {
    PROPERTY,
    ROOM,
    TRIP,
    OTHER
}
